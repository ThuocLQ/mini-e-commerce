# Active Path Migration Map

The learning material records the repository layout that existed when each lesson was written. The current runnable paths are below. Apply this map when a lesson command uses an older path.

| Historical path | Current runnable path |
| --- | --- |
| `Services/ApiGateway` | `Gateways/ApiGateway` |
| `Services/NotificationWorker` | `Workers/NotificationWorker` |
| `Services/ProjectionWorker` | `Workers/ProjectionWorker` |
| `BuildingBlocks/BuildingBlocks.Contracts` | `BuildingBlocks.Contracts` |

Runtime source of truth is `README.md` and `docs/`. Lessons remain historical learning material; do not use their paths as deployment configuration.