import { CheckoutClient } from "./checkout-client";

export const metadata = {
  title: "Checkout | MicroShop",
  description: "Review your order and complete checkout.",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}